export enum StatusEnum {
  ACTIVE = 'active',
  PENDING = 'pending',
  SUSPENDED = 'suspended',
  BLOCKED = 'bloked',
  REMOVED = 'removed',
  EXPIRED = 'expired',
  REJECTED = 'rejected',
  PAUSED = 'paused',
  CLOSED = 'closed',
}
